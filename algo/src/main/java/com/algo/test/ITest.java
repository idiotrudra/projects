package com.algo.test;

/**
 * Interface for creating test classes which can be executed with a common main
 * instead of creating its own main class.
 * 
 * @author rudra
 *
 */
public interface ITest {

	void PerformTest();
}
